import { LanguagesModel } from './Languages.models';
import { CorusesModel } from './Courses.models';
import { ToolsModel } from './Tools.models';

export namespace AppItems {
  export const Languages = LanguagesModel.LANGUAGES;
  export const Coruses = CorusesModel.CORUSES;
  export const Tools = ToolsModel.TOOLS;
}